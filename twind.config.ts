import { Options } from "$fresh/plugins/twind.ts";

export default {
  selfURL: import.meta.url,
  theme: {
    colors: {
      pallete: {
        dark: "#000022",
        light: "#fbf5f3",
        mustard: "#e28413",
        brick: "#de3c4b",
        coral: "#c4287"
      }
    }
  }
} as Options;
